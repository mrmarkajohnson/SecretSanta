@model string

@if (Model.IsNotEmpty())
{
    <div class="d-inline" data-toggle="tooltip" data-html="true" data-bs-html="true" data-bs-container="label" title="@Model">
        <i class="fa fa-circle-question fa-border"></i>
    </div>
}